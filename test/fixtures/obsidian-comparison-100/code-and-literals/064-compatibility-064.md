# Compatibility 064

Fixture path: `code-and-literals/064-compatibility-064.md`.

## Code and literals

Inline `const value = 4;` and escaped punctuation.

```javascript
function fixture64(value) {
  return value * 4;
}
```

```text
single line 4
```

    indented code 4

Paragraph 1 for fixture 64 contains **bold**, *emphasis*, ~~deleted text~~, ==highlighted text==, and `inline code`.

Paragraph 2 for fixture 64 contains **bold**, *emphasis*, ~~deleted text~~, ==highlighted text==, and `inline code`.

Paragraph 3 for fixture 64 contains **bold**, *emphasis*, ~~deleted text~~, ==highlighted text==, and `inline code`.

Paragraph 4 for fixture 64 contains **bold**, *emphasis*, ~~deleted text~~, ==highlighted text==, and `inline code`.
